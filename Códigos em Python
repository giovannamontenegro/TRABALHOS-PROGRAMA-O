Código de Repetição: # Loop que vai de 1 até 10
for i in range(1, 11):
    if i % 2 == 0:
        print(f"{i} é par")
    else:
        print(f"{i} é ímpar")

Código de Tomada de Decisão: # Solicita ao usuário que insira um número
numero = float(input("Digite um número: "))

# Estrutura de decisão para verificar se o número é positivo, negativo ou zero
if numero > 0:
    print("O número é positivo.")
elif numero < 0:
    print("O número é negativo.")
else:
    print("O número é zero.")


